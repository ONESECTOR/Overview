package com.sector.domain.repository.firebase

import com.sector.domain.entity.firebase.User
import kotlinx.coroutines.flow.Flow

interface FirebaseFirestoreRepository {

    //val user: Flow<User>

    //suspend fun createUser(): Flow<User>
}